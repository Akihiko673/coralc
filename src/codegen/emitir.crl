mod libc = import("std/platform/libc.crl");

// emitir.crl: WallVM IR emission (not yet implemented).

pub void emitIR(check.Check* ck, str.strView path, u32 optLevel, bool debug)
{
    libc.write(2, (const u8*)"coral: --emit-ir: WallVM IR emission not yet implemented\n", 56);
}
