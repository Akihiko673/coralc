in this one, wallvm should be used as the bakend.
not, the borrow checking and loop unrolling are opt-in, not enabled by default. the number optimizations on the other hand, it will be in 01 by default. O0 on the other hand, will not contain optimizations, only removing unreachable code, anad things that are not being used, that will make the binary size larger. there should also be a --debug flag, this will enable debug information in the binary, such as line numbers, variable names, and other debug information, and will be in 01. the debug sympols should be visible in the assembly, linker and final exe, so that gdb can be used to debug the binary.
